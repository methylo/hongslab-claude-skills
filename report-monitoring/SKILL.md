---
name: report-monitoring
description: >
  월간 모니터링 보고서 작성 스킬. 월간 데이터(표·수치·메모)를 입력받아
  현황 요약 → 주요 이슈 → 조치 사항 → 다음 달 계획 4단계 구조의 보고서 초안을 생성한다.
  사회복지·공공기관 실무자가 매달 반복 작성하는 정기 보고에 특화되어 있다.
  트리거: 모니터링 보고서, 월간 보고서, 월간 현황 보고, 월별 보고, 월간 실적,
  이번 달 보고, 정기 모니터링, 서비스 모니터링, 활동 보고서, 관리 보고서.
  유형이 불분명한 보고서 요청은 report-suite가 판별한다.
  주간 보고는 report-weekly, 회의록은 meeting-minutes,
  리서치·분석 보고서는 research-report,
  공공 표준 양식(.hwpx) 공고문·보고서는 chungcheong-report를 사용한다.
---

# 월간 모니터링 보고서 스킬

데이터를 입력하면 구조화된 월간 보고서 초안을 생성하는 스킬이다.

## 이 스킬이 하는 일

사용자가 업무 데이터(표, 수치, 메모)를 붙여넣으면, 4단계 구조의 보고서 초안을 작성한다. AI가 만든 초안은 출발점이다. 사용자가 사실관계를 확인하고 수정하여 최종본을 완성한다.

## 공용 기준

이 스킬은 `report-suite`의 공용 기준을 상속한다. 아래 파일을 먼저 읽고 적용한다.

| 파일 | 적용 시점 |
|---|---|
| `../report-suite/references/report-standards.md` | 2단계 초안 작성 전, 3단계 수정 후 |
| `../report-suite/references/output-formats.md` | 4단계 파일 저장 시 |

`report-suite` 폴더 없이 이 스킬만 단독으로 사용하는 경우, 아래 "최소 규칙"으로 진행한다.

**최소 규칙:** 개인정보는 가명 처리 / 종결어미 `~하였다`체 통일 / 한 문장 40자 이내 / 주장에는 수치 근거 병기 / 입력에 없는 수치 생성 금지 / 금지어(혁신적인·매우·상당히) 사용 금지.

---

## 전체 작업 흐름

이 스킬은 4단계로 진행된다. 각 단계가 끝날 때마다 현재 위치와 다음 할 일을 안내한다.

```
1단계: 입력 확인 — 데이터를 받고 보고서 방향을 정한다
   ↓
2단계: 보고서 초안 작성 — 4단계 구조로 초안을 만든다
   ↓
3단계: 검토 및 수정 — 사용자가 확인하고 수정을 요청한다
   ↓
4단계: 파일 저장 — 워드(.docx) 또는 한글(.hwpx)로 저장한다
```

## 1단계: 입력 확인

사용자에게 아래 정보를 확인한다. 모든 항목이 없어도 있는 것만으로 진행할 수 있다.

**필수 입력:**
- 보고서 제목 또는 주제 (예: "4월 활동지원서비스 모니터링 보고서")
- 데이터 (표, 수치, 텍스트 중 어떤 형태든 가능)

**선택 입력:**
- 보고 대상 (상급 기관, 내부 회의 등)
- 이전 달 보고서 또는 비교 기준
- 강조하고 싶은 이슈나 성과

사용자가 데이터만 붙여넣고 별다른 설명이 없으면, 데이터를 분석하여 보고서 주제와 구조를 먼저 제안한다.

**1단계 완료 후 안내 멘트:**

> 데이터를 확인했습니다. 지금부터 보고서 초안을 작성합니다.
>
> **진행 상황:** 1단계(입력 확인) ✅ → **2단계(초안 작성)** 진행 중 → 3단계(검토) → 4단계(파일 저장)

---

## 2단계: 보고서 초안 작성

아래 4단계 구조로 보고서를 작성한다.

```
[보고서 제목]
작성일: YYYY년 MM월 DD일
작성자: (사용자가 지정하면 기입, 미지정 시 공란)

1. 현황 요약
   - 핵심 수치 3~5개를 문장으로 정리
   - 전월 대비 변화가 있으면 비교 포함
   - 표로 정리할 수 있는 데이터는 표로 제시

2. 주요 이슈
   - 데이터에서 도출되는 문제점 또는 특이사항 2~3건
   - 각 이슈에 근거 수치를 함께 제시

3. 조치 사항
   - 각 이슈에 대한 대응 방안
   - 이미 조치한 내용과 향후 계획을 구분

4. 다음 달 계획
   - 중점 관리 항목 2~3건
   - 구체적 일정이나 목표 수치가 있으면 포함
```

초안을 작성한 뒤 아래 "자가 점검"을 실행한다. 점검을 통과한 초안만 사용자에게 제시한다.

**2단계 완료 후 안내 멘트:**

> 보고서 초안이 완성되었습니다.
>
> **진행 상황:** 1단계(입력 확인) ✅ → 2단계(초안 작성) ✅ → **3단계(검토)** ◀ 현재 → 4단계(파일 저장)
>
> **지금 할 일:** 아래 초안을 확인해 주세요.
> - 수치나 사실관계가 다른 부분이 있으면 알려주세요
> - 추가하거나 빼고 싶은 내용이 있으면 말씀해 주세요
> - 톤이나 분량 조정이 필요하면 요청해 주세요
>
> **다음 단계:** 내용이 확정되면, 워드(.docx) 또는 한글(.hwpx)로 파일을 저장합니다.

---

## 3단계: 검토 및 수정

사용자가 수정을 요청하면 반영한다. 수정된 내용에도 자가 점검을 다시 실행한다.

사용자가 "이대로 좋다", "확인했다", "저장해줘" 등 확정 의사를 밝히면 4단계로 넘어간다.

**3단계 완료 후 안내 멘트:**

> 보고서 내용이 확정되었습니다.
>
> **진행 상황:** 1단계 ✅ → 2단계 ✅ → 3단계 ✅ → **4단계(파일 저장)** ◀ 현재
>
> 보고서를 파일로 저장할까요? 아래에서 형식을 선택해 주세요.
> - **워드(.docx)** — MS 워드에서 편집 가능
> - **한글(.hwpx)** — 한컴오피스에서 편집 가능
> - **저장 안 함** — 위 텍스트를 직접 복사해서 사용

---

## 4단계: 파일 저장

사용자가 선택한 포맷으로 파일을 생성한다.

- **워드(.docx)** 선택 시: docx 스킬을 사용하여 보고서를 워드 문서로 생성
- **한글(.hwpx)** 선택 시: hwpx 스킬을 사용하여 보고서를 한글 문서로 생성
- 사용자가 파일 저장을 원하지 않으면 대화창 텍스트 그대로 종료

파일명·저장 위치는 `../report-suite/references/output-formats.md`를 따른다. 기본 형식은 `YYYY년 MM월 [보고서 제목].docx`이다.

**4단계 완료 후 안내 멘트:**

> 파일이 생성되었습니다.
>
> **전체 진행:** 1단계 ✅ → 2단계 ✅ → 3단계 ✅ → 4단계 ✅ 완료
>
> 같은 데이터로 다른 형태의 문서가 필요하면 알려주세요.
> - "짧게 요약해줘" → 반 페이지 요약 보고
> - "발표용으로 바꿔줘" → 슬라이드용 불릿 포인트
> - "건의서로 바꿔줘" → 현황-문제점-개선안-기대효과 구조

---

## 이 스킬 고유 규칙

문체·수치 표기·금지어는 공용 기준(`report-standards.md`)을 따른다. 이 스킬만의 규칙은 아래 두 가지다.

- **분량** 기본 A4 1~2페이지. 사용자가 지정하면 그에 맞춘다
- **비교 기준** 전월 대비 변화를 반드시 포함한다. 이전 달 자료가 없으면 "비교 기준 없음"으로 명시한다

---

## 입력 데이터 처리

사용자가 다양한 형태로 데이터를 줄 수 있다. 각 경우에 맞게 처리한다.

**표 데이터:** 그대로 분석하여 현황 요약에 핵심 수치를 추출한다
**텍스트 메모:** 구조화되지 않은 메모에서 핵심 내용을 뽑아 보고서 구조에 배치한다
**이전 보고서:** 형식과 톤을 참조하여 일관성을 유지한다
**복수 데이터:** 여러 출처의 데이터를 통합하여 하나의 보고서로 정리한다

---

## 보고서 유형 변형

기본은 월간 모니터링 보고서이지만, 사용자가 요청하면 같은 데이터로 다른 형태의 문서도 작성할 수 있다.

- **요약 보고:** 핵심 수치와 이슈만 반 페이지로 압축
- **발표용 요약:** 슬라이드에 넣을 수 있는 불릿 포인트 형태
- **상급 기관 보고:** 현황-문제점-개선안-기대효과 구조의 건의서 형태

사용자가 "짧게", "발표용으로", "건의서로" 등의 키워드를 쓰면 해당 변형을 적용한다.

---

## 자가 점검

초안 완성 후 `../report-suite/references/report-standards.md`의 "자가 평가 루프"를 실행한다. 점검 결과를 보고서 말미에 한 줄로 표기한 뒤 사용자에게 제시한다.

> 점검 완료: 개인정보 0건 / 금지어 0건 / 수치 근거 확인 / AI 흔적 0건 / 문체 통일(~하였다)

공용 기준 파일이 없으면 위 "최소 규칙"으로 점검한다.
