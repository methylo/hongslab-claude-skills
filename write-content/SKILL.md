---
name: write-content
description: >
  칼럼형·문서형 단편 콘텐츠를 메모나 주제에서 완성 원고로 만든다.
  기본 산출물은 페이스북·카카오톡에 변환 없이 붙여넣는 평문이며, 블로그 확장본을 옵션으로 낸다.
  문체 기준은 내장하지 않고 hong-voice-reference를 호출해 적용한다.
  채널이 정해진 글은 write-router가 배정한 스킬을 사용한다.
  트리거: 글쓰기, 아티클, 뉴스레터, 보고서, 보도자료, 기고문, 발표 원고, 강의 스크립트,
  카피라이팅, 메모 정리, 문서 작성.
---

# 글쓰기 워크플로우 (Writing Workflow)

단편 콘텐츠를 위한 5단계 구조적 글쓰기 프로세스. 메모·주제 기반 입력을 칼럼형 또는 문서형 글로 변환한다. 기본 산출물은 페이스북·카카오톡에 바로 게시하는 평문 텍스트다.

## 보조 파일 안내

이 스킬은 워크플로우 흐름을 제공하며, 상세 규칙은 보조 파일에서 참조한다.

| 보조 파일 | 내용 | 참조 시점 |
|----------|------|----------|
| `references/writing-principles.md` | 공통 집필 원칙, 금지어 규칙, 분량 통제, 단어 선택 기준 | Step 3 초안 작성 전, Step 4 리라이팅 전 |
| `references/tone-guide.md` | 칼럼형 vs 문서형 톤 차이 비교, 유형별 톤 적용 규칙, 도입부 작성법 | Step 2 구조 설계 시, Step 3 초안 작성 시 |
| `references/publish-format.md` | 기본본(페북·카톡) 평문 규칙, 첫 2줄 후크, 확장본(블로그) 변환 규칙 | Step 3 초안 작성 시, Step 5 최종 출력 시 |
| `references/review-checklist.md` | Step 4 점검 1~5 상세 체크리스트 | Step 4 리라이팅 시 |
| `references/output-rules.md` | 단계별 산출물 명세, 기본본·확장본 포맷 규칙, 산출물 총괄 | 각 Step 산출물 작성 시 |

**읽기 규칙:** 각 Step 진입 시 해당 보조 파일을 읽는다. 이미 같은 대화에서 읽은 파일은 다시 읽지 않는다.

---

## 문체 기준 로드 (해당 대화에서 1회)

문체·표현·AI 흔적 기준은 이 스킬이 소유하지 않는다. `hong-voice-reference` 스킬이 정본이다.

1. `Skill` 도구로 `hong-voice-reference`를 호출한다.
2. 반환된 `Base directory`를 BASE로 두고 필요한 층만 읽는다.

```text
{BASE}/references/01-voice.md         Step 3 초안 작성 전, Step 4 리라이팅 전 (필수)
{BASE}/references/02-glossary.md      Step 3 단어 선택 시, Step 4 점검 2 (필수)
{BASE}/references/03-anti-traces.md   Step 3 자가 점검, Step 4 점검 2, Step 5 출력 직전 (필수)
{BASE}/references/04-examples.md      문체 판정이 갈릴 때만
{BASE}/references/05-corrections.md   같은 표현을 두 번 이상 고쳤을 때만
```

3. 층 순서를 지킨다. 1층 시선·구조 → 2층 단어 → 3층 흔적 제거.
4. 같은 대화에서 이미 읽은 파일은 다시 읽지 않는다.
5. 호출이 실패하면 이 문서 하단 `문체 최소 카드`로 진행한다.

### 실행 모드 표기 (필수)

Step 3·4·5의 모든 산출물 말미에 아래 한 줄을 **항상** 붙인다. 성공·실패 모두 적는다. 이 줄이 없으면 문체 기준을 로드하지 않은 것이다.

```text
문체 기준: hong-voice-reference (1층·2층·3층 적용)
문체 기준: 폴백 최소 카드 (hong-voice-reference 호출 실패)
```

본문에서 설명하지 않는다. 한 줄로 끝낸다.

---

## 전체 흐름

```
Step 1: 목적 정의 → Step 2: 구조 설계 → Step 3: 초안 작성 → Step 4: 리라이팅 → Step 5: 최종 출력
```

사용자의 상태에 따라 적절한 단계부터 시작한다.
- 주제만 있음 → Step 1부터
- 메모·원문 텍스트 있음 → Step 1부터 (원문 보존 모드 적용)
- 구조/아웃라인 있음 → Step 3부터
- 초안 있음 → Step 4부터

### Step 간 전환 규칙

각 Step 완료 후, 다음 Step으로 넘어가기 전에 사용자 확인을 받는다.

| 전환 | 확인 방식 |
|------|----------|
| Step 1 → 2 | 기본 프로파일 적용 여부 + 주제·유형 요약 제시 → 확정 여부 확인 |
| Step 2 → 3 | 아웃라인 제시 → 구조 수정 여부 확인 |
| Step 3 → 4 | 초안 제시 → 방향 수정·추가 요청 확인 |
| Step 4 → 5 | 수정본 제시 → 추가 수정 여부 확인 |

"끝까지 진행해줘" 또는 "한 번에 해줘" 요청 시 확인 없이 연속 진행한다.

---

## 홍작가 기본 프로파일 (내장 기본값)

별도 언급이 없으면 아래 기본값을 적용한다. Step 1에서 매번 다시 묻지 않는다.

| 항목 | 기본값 |
|------|--------|
| 독자 | AI 활용 실무자·작가·강사 (클로드·코워크·글쓰기 자동화에 관심 있는 층) |
| 1차 채널 | 페이스북 + 카카오톡 (평문 그대로 동시 게시) |
| 2차 채널 | sshong.com 블로그 (확장본, 요청 시) |
| 콘텐츠 유형 | 칼럼형 우선 |
| 종결어미 | ~합니다체 (페이스북·에세이는 원문 호흡에 맞춤, `01-voice.md` §8-1) |
| 출력 포맷 | 평문 텍스트 (마크다운 기호 없음, `references/publish-format.md` 기준) |

**기본값 해제 신호:** 보고서·보도자료·기고문·뉴스레터 등 다른 유형 언급, "블로그용으로", "docx로", 독자·채널을 직접 지정하는 발화. 이 경우 해당 항목만 교체하고 나머지 기본값은 유지한다.

---

## 콘텐츠 유형 체계

### 핵심 유형

| 유형 | 정의 | 기본 분량 |
|------|------|----------|
| **칼럼형** | 하나의 관점을 세우고 논증하는 글. 현상 관찰 → 문제 재정의 → 기준 제시 → 관점 전환 | 1,500~2,500자 |
| **문서형** | 메모·주제·자료를 서론-본론-결론 구조로 정리하는 글. 정보 전달과 논리 정리 목적 | 2,000~4,000자 |

### 보조 유형

| 유형 | 기본 분량 |
|------|----------|
| 뉴스레터 | 800~1,500자 |
| 분석 기사/심층 아티클 | 4,000~6,000자 |
| 보고서/제안서 | 2,000~5,000자 |
| 보도자료 | 800~1,200자 |
| SNS 롱폼 | 1,000~2,000자 |
| 강의 스크립트 | 분당 약 300자 |

### 전용 스킬 분리 유형 (이 스킬에서 제외)

블로그(sshong.com) 전용 SEO 글 → write-blog / 페이스북 전용 유형 설계 → write-facebook / 브런치 → write-brunch / 에세이 → write-essay

단, 기본 프로파일에 따른 페북·카톡 동시 게시용 평문 산출은 이 스킬의 기본 범위다. write-facebook은 페이스북 알고리즘·7유형·댓글 설계가 별도로 필요할 때만 사용한다.

---

## Step 1: 목적 정의 (Purpose Definition)

### 기본 진행 (기본 프로파일 적용)

기본 프로파일이 유효한 일반 요청에서는 질문을 1개로 줄인다.

1. 독자·채널·유형·포맷에 기본 프로파일을 적용한다
2. **주제·목적 확인 1개 질문만 한다**: "이 글로 독자의 어떤 판단을 바꾸려는 건가요?" (주제가 이미 명확하면 질문 생략)
3. 적용한 기본값을 요약 1줄로 제시하고 Step 2로 진행한다

### 예외 진행 (기본값 해제 신호 감지 시)

보고서·보도자료 등 다른 유형이거나 독자·채널이 다른 경우, 아래 항목 중 미확인 항목만 질문한다.

| 항목 | 질문 |
|------|------|
| **목적** | 이 글로 무엇을 달성하려는가? |
| **독자** | 누가 읽는가? 현재 수준은? |
| **채널** | 어디에 게시하는가? |
| **콘텐츠 유형** | 칼럼형 / 문서형 / 보조 유형 중 선택 |

추측으로 채우지 않고 질문한다. 질문은 한 번에 하나씩 한다.

### 유형 자동 판별 기준

사용자가 유형을 명시하지 않은 경우:

| 신호 | 판별 유형 |
|------|----------|
| "내 생각은~", 관점·의견 중심 | 칼럼형 |
| 메모·자료 제공, "정리해줘", "문서로 만들어줘" | 문서형 |
| "분석해줘", "심층적으로" | 분석 기사 |
| "보고서", "제안서" | 보고서 |

### 산출물

적용된 프로파일(주제·독자·채널·유형·포맷)을 요약 정리해 사용자에게 제시. 확인 후 Step 2로. (산출물 형식 상세는 `references/output-rules.md` 참조)

---

## Step 2: 구조 설계 (Structure Design)

> **이 단계 진입 시** `references/tone-guide.md`를 읽는다.

Step 1에서 확정된 유형에 따라 구조를 설계한다.

**원문 보존 모드인 경우:** 원문의 흐름에서 구조를 추출한다. 유형별 표준 구조를 강제 적용하지 않고, 원문의 문단 순서를 기본 골격으로 삼은 뒤 부족한 부분(도입·전환·마무리)만 보강 방향을 제시한다.

### 칼럼형 구조

```
도입: 첫 2줄 후크 (현상 관찰 또는 통념 제기, publish-format.md 기준)
전환: 문제 재정의 (왜 그 통념이 흔들리는지)
기준: 새로운 판단 기준 제시 + 근거/사례 (1~2개)
마무리: 관점 재정렬 (요약이 아니라 질문의 전환)
```

### 문서형 구조 (서론-본론-결론)

```
서론: 배경·맥락 설명 + 이 글의 범위 명시
본론 1: 핵심 개념/항목 설명 + 판단 기준
본론 2: 적용 방식/사례/데이터 + 해석
본론 3: 심화 또는 주의사항 (선택)
마무리: 현재 판단·달라진 행동·현재 작업 상태·남은 질문 중 하나 (01-voice §6)
        절차 안내·연락처 같은 기능 블록이 필요하면 마무리 문단 밖에 별도로 둔다
```

### 보조 유형 구조

**뉴스레터:** 인사/도입 → 메인 콘텐츠(1~2주제) → 부가 정보 → CTA + 다음 호 예고

**분석 기사:** 도입(문제 재정의) → 본론1(판단 기준) → 본론2(메커니즘) → 본론3(사례) → 관점 재정렬

**보고서/제안서:** 요약 → 배경/현황 → 분석/핵심 내용 → 제안/권고 → 기대 효과 → 부록

### 산출물

아웃라인을 사용자에게 제시. 확인 후 Step 3으로. (산출물 형식 상세는 `references/output-rules.md` 참조)

---

## Step 3: 초안 작성 (First Draft)

> **이 단계 진입 시** `references/writing-principles.md`, `references/publish-format.md`를 읽는다.
> (Step 2에서 `references/tone-guide.md`를 이미 읽었다면 다시 읽지 않는다.)
>
> **문체 기준**: 위 `문체 기준 로드` 절차로 `hong-voice-reference`를 호출하고 `01-voice.md`(1층)·`02-glossary.md`(2층)·`03-anti-traces.md`(3층)를 읽는다. 시선·전개 순서·문단 리듬은 1층, 단어 선택은 2층, 금지어·상투어 판정은 3층을 따른다.
>
> 3층을 함께 읽는 이유: 아래 초안 자가 점검이 과장형 형용사·모호한 부사·완곡 표현을 검사하는데, 이는 3층 소관이다. 3층 없이 점검하면 §2의 부사 유지 조건과 §3의 불확실성 보존 예외를 적용하지 못해 과교정이 발생한다.

### 초안 작성 모드 판별

사용자 입력에 따라 두 가지 모드 중 하나를 적용한다.

| 입력 상태 | 모드 | 설명 |
|----------|------|------|
| 주제·키워드만 제공 | **새로 쓰기 모드** | 아웃라인 기반으로 문단을 새로 작성한다 |
| 메모·초안·원문 텍스트 제공 | **원문 보존 모드** | 원문의 문장·어투·흐름을 골격으로 유지하고, 구조 보강과 문장 정리만 수행한다 |

메모 분량이 3문장 이상이면 원문 보존 모드를 기본 적용한다. 사용자가 "새로 써줘", "다시 작성해줘"라고 명시한 경우에만 새로 쓰기 모드로 전환한다.

### 원문 보존 모드 규칙

원문 보존 모드에서는 다음 규칙을 따른다.

**보존 대상 (건드리지 않는 것):**
- 원문의 핵심 문장과 표현 — 사용자의 어투, 말투, 어휘 선택을 그대로 유지한다
- 원문의 서술 순서 — 사용자가 배치한 문단 흐름을 기본 골격으로 삼는다
- 원문의 감정·온도 — 솔직한 표현, 감탄, 체험적 어조가 있으면 살린다
- 원문의 구체적 사례·경험 — 추상화하거나 일반화하지 않는다

**수정 대상 (다듬는 것):**
- 문장 단위 정리 — 한 문장에 여러 정보가 섞인 경우 분리한다
- 접속 관계 보강 — 문단 간 전환이 끊기는 곳에 연결 문장을 추가한다
- 구조 보강 — 소제목 배치, 도입·마무리 보강. 단, 원문의 흐름 안에서 수행한다
- 금지어·AI 흔적 — writing-principles.md 기준에 따라 교체하되, 원문 어투의 범위 내에서 대체 표현을 찾는다

**금지 행위:**
- 원문에 없는 개념·사례·설명을 새로 추가하지 않는다
- 원문의 어투를 표준 문어체로 전면 교체하지 않는다
- 원문의 문단 순서를 아웃라인에 맞추기 위해 재배치하지 않는다

### 출력 슬롯 템플릿 (초안은 반드시 이 구조를 따른다)

Step 2에서 확정한 유형별 구조를 슬롯 템플릿으로 강제 적용한다. 슬롯을 건너뛰거나 순서를 바꾸지 않는다.

**칼럼형:**
```
[도입] ← 첫 2줄 후크 + 현상 관찰 또는 통념 제기 (1~2문단)
[전환] ← 문제 재정의 (1문단)
[기준] ← 새로운 판단 기준 + 근거/사례 (2~3문단)
[마무리] ← 관점 재정렬 (1문단, 요약 아님)
```

**문서형:**
```
[서론] ← 배경·맥락 + 범위 명시 (1~2문단)
[본론1] ← 핵심 개념/항목 + 판단 기준
[본론2] ← 적용 방식/사례/데이터 + 해석
[본론3] ← 심화 또는 주의사항 (선택)
[결론] ← 핵심 요약 3문장 이내 + CTA
```

### 첫 2줄 후크 규칙 (기본본 필수)

페이스북 "더보기" 접힘과 카카오톡 미리보기에서 첫 1~2줄이 클릭을 결정한다. 상세 기준은 `references/publish-format.md` 참조.

1. 첫 문장은 단문(7어절 이하)으로 쓴다. 인사말·자기소개·배경 설명으로 시작하지 않는다
2. 둘째 줄이 끝나기 전에 긴장을 만든다 — 통념의 균열, 뜻밖의 관찰, 구체적 장면 중 하나
3. 첫 2줄 안에 기호·이모지·해시태그를 넣지 않는다

### 감성 레이어 (칼럼형·SNS 롱폼 기본 적용)

논증 골격 위에 3지점(도입 장면 1컷 → 장면 뒤 감정 1문장 → 마무리 여운 1문장)을 배치한다. 상세 규칙과 가드레일은 `references/tone-guide.md`의 감성 레이어 섹션 참조.

- 소재가 경험·장면 기반이면 2~3지점, 개념·분석 기반이면 도입 장면만 적용한다
- 감정 문장은 글 전체 2개 이내. 감탄·과장 금지, 상태 서술로 표현한다
- 문서형·보고서·보도자료에는 적용하지 않는다

### 평문 기본 원칙 (기본본 필수)

기본본은 페이스북·카카오톡에 복사 후 그대로 게시하는 평문이다. 상세 기준은 `references/publish-format.md` 참조.

- 마크다운 기호(#, ##, **, -, 1., >) 사용 금지
- 소제목은 부호 없는 본문형 한 줄로 쓰고 위아래 빈 줄로 구분한다
- 단락 사이 빈 줄 1줄
- 표·번호 리스트·볼드 금지. 순서가 필요하면 "첫째, 둘째"로 서술한다

### 새로 쓰기 모드 (기존 프로세스)

Step 2의 아웃라인을 따라 **문단 단위**로 작성한다.

**문단 작성 3단계:**
1. 이 문단의 역할 결정 — 관찰/전환/기준/사례/정리 중 무엇인가
2. 첫 문장 단문으로 작성 — 이 문단이 다루는 대상을 한 문장으로 제시
3. 후속 문장 점층 배치 — 뒤로 갈수록 판단 밀도가 높아지도록

### 도입부 작성법

유형별 도입부 작성 패턴은 `references/tone-guide.md`에 정의되어 있다.

### 금지어 실시간 체크

**문장 생성 시 즉시 점검** (판정 기준은 `03-anti-traces.md`, 예외 조항을 함께 적용한다):

1. "~할 수 있습니다" — 가능 조건이 분명하면 그대로 둔다. 근거 없는 가능성일 때만 조건·행동·판단으로 바꾼다 (§3)
2. "매우·쉽게·빠르게·상당히·꽤" — 실제 체감이나 전후 비교를 나타내면 그대로 둔다. 근거 없이 강도만 높일 때 수치·단계·조건으로 교체한다 (§2)
3. 형용사가 근거 없이 쓰였는가 → 비교 대상·수치 추가 또는 삭제 (§1)
4. 추상 명사가 문장 중심에 있는가 → 동사 중심 문장으로 전환
5. 판단이 드러나는가 → "그래서 어떻다는 건가?" 테스트. 단, 모든 문단 끝에 판단문을 붙이지는 않는다 (`01-voice.md` §3-1)

출현만으로 위반으로 세지 않는다. 반복 밀도와 문맥을 함께 본다 (§5-1).

### 본론 작성 규칙

**칼럼형:** 하나의 판단 기준 → 왜 필요한지 설명 → 사례로 증명. 사례 나열 금지. 섹션 마지막은 "기준은 ~이다"로 수렴.

**문서형:** 항목별 개념 → 적용 → 주의사항. 기본본에서는 번호 리스트 대신 "첫째, 둘째" 서술을 쓴다. 확장본·문서 파일에서만 번호 리스트를 허용하되 각 항목 1문장 이상 해설. 수치는 해석 문장과 함께 배치.

### 초안 자가 점검 (출력 전 반드시 실행)

초안 작성 완료 후, 아래 항목을 하나씩 확인한다. 해당 항목이 있으면 해당 문장을 수정한 뒤 출력한다.

- [ ] `03-anti-traces.md` §1~§4 위반 여부 (과장형 형용사·모호한 부사·완곡 표현·빈 연결어). **§2 부사 유지 조건과 §3 불확실성 보존 예외를 함께 적용한다**
- [ ] `03-anti-traces.md` §10 상투 표현 사전 해당 여부 (§5-1 판정 원리 적용 — 출현만으로 위반이 아님)
- [ ] 추상 명사 중심 문장("중요성", "필요성") 포함 여부
- [ ] 슬롯 구조 이탈 여부 (블록 순서 변경, 슬롯 누락)
- [ ] 칼럼형: 마무리가 요약이 아닌 관점 재정렬인가?
- [ ] 기본본: 마크다운 기호·볼드·표 잔존 여부, 첫 2줄 후크 충족 여부
- [ ] **`hong-voice-reference`의 `01-voice.md`·`02-glossary.md`를 이번 실행에서 읽었는가** (읽지 않았으면 지금 호출한 뒤 초안을 다시 점검한다)
- [ ] 산출물 말미에 실행 모드 한 줄을 붙였는가

### 문장 품질 기준 (GOOD/BAD 예시)

규칙보다 예시가 출력 품질에 더 큰 영향을 준다. 초안 작성 시 아래 쌍을 기준으로 삼는다.

| BAD (금지) | GOOD (기준) |
|-----------|------------|
| AI를 활용하면 쉽게 콘텐츠를 만들 수 있습니다 | AI에 원고 구조를 입력하면, 초안 생성 시간이 3시간에서 40분으로 줄어든다 |
| 혁신적인 방법으로 업무 효율을 높일 수 있습니다 | 반복 보고서 3건을 템플릿화하자, 주간 작성 시간이 절반이 됐다 |
| 이 도구는 다양한 기능을 제공하여 업무에 큰 도움이 됩니다 | 이 도구는 1회 입력으로 제목, 소제목, 본문 초안을 동시에 생성한다 |

### 산출물

초안은 대화창 텍스트로 직접 제공한다. 기본본은 평문 규칙을 적용한 상태로 제시한다. 파일 저장은 명시 요청 시에만. 자가 점검 전 항목과 슬롯 구조 충족 후 출력. 상세는 `references/output-rules.md` 참조.

---

## Step 4: 리라이팅 (Rewriting)

> **리라이팅 원본 보존 원칙** (`references/writing-principles.md` 참조): 리라이팅은 내용을 바꾸지 않는다. 같은 내용을 더 정확하고 읽기 좋은 문장으로 다듬는다. 구조 변경이 필요하면 별도 제안으로 분리한다.

> **이 단계 진입 시** `references/review-checklist.md`를 읽는다.
>
> **문체 기준**: `hong-voice-reference`의 `01-voice.md`·`02-glossary.md`·`03-anti-traces.md`를 1층 → 2층 → 3층 순서로 적용한다. 점검 3(스타일)은 `01-voice.md` §11 이탈 징후와 §3 리듬을, 점검 2의 AI 흔적 부분은 `03-anti-traces.md`를 기준으로 판정한다.

### 역할 전환 선언 (리라이팅 진입 조건)

이 단계에서는 초안 작성자의 시점을 내려놓는다. 편집자로서, 작성 의도를 참고하지 않고 **독자 시점에서만 판단**한다. "왜 이렇게 썼는가"가 아니라 "독자가 읽었을 때 작동하는가"를 기준으로 점검한다. 이 선언 없이 체크리스트를 돌리면, 구조적 문제보다 표현만 다듬는 쪽으로 흐른다.

### 진행 방식

1. **점검 리포트 제시** — 5가지 점검 결과를 표 형태로 제시
2. **사용자 확인** — 수정 방향 동의 또는 추가 지시
3. **수정본 작성** — 확인된 방향에 따라 전체 수정본 제시
4. **변경 요약** — 수정본 하단에 주요 변경 사항 정리

"리라이팅해줘" → 1~4 한 번에 진행. "점검만 해줘" → 1까지만 진행.

### 점검 항목 (상세는 review-checklist.md)

| # | 점검 영역 | 핵심 |
|---|----------|------|
| 1 | 구조 점검 | 도입·흐름·소제목·마무리가 유형에 맞는가 |
| 2 | 문장 점검 + AI 흔적 진단 | 단어 수, 종결어미, 금지어·상투 표현 사전, 접속어·번역투·마무리 과장, AI 흔적 지수(전/후) |
| 3 | 스타일 점검 | 홍작가 스타일 일치, 문장 리듬 교차, 감성 레이어 작동 여부 |
| 4 | 채널 최적화 | 분량, 톤, 제목, CTA, 출처 |
| 5 | 발행 포맷 점검 | 기본본 평문 규칙 충족, 첫 2줄 후크 작동 여부 |

점검 2는 edit-deai의 진단 체계를 이식한 것이다. 과교정 방지 원칙(AI 흔적이 분명한 문장만 수정, 작성자 고유 문체 보존)을 함께 적용한다. write-content로 완성한 글에는 edit-deai를 별도로 실행하지 않는다.

### 변경 요약 포맷

```
**주요 변경 사항:**
- 구조: (변경 내용)
- 문장: (변경 내용)
- 스타일: (변경 내용)
- 채널·포맷: (변경 내용)

AI 흔적 지수: 수정 전 N.N (등급) → 수정 후 N.N (등급)
문체 기준: hong-voice-reference (1층·2층·3층 적용)
```

마지막 두 줄은 생략하지 않는다. AI 흔적 지수 산출 기준은 `03-anti-traces.md` §11을 따른다.

---

## Step 5: 최종 출력 (Final Output)

> **이 단계 진입 시** `references/publish-format.md`를 읽는다. (이미 읽었다면 생략)
>
> **문체 기준**: 출력 직전에 `hong-voice-reference`의 `03-anti-traces.md` 최종 점검 체크리스트를 1회 통과시킨다. 이 통과는 아래 진행 방식의 필수 단계이며 생략하지 않는다. Step 4를 건너뛰고 Step 5로 바로 들어온 경우 `01-voice.md`·`02-glossary.md`도 함께 읽고, 흔적 지수는 '수정 전' 값 없이 현재 값만 보고한다.

### 원소스 2버전 구조

| 버전 | 용도 | 생성 시점 |
|------|------|----------|
| **기본본** | 페이스북 + 카카오톡 동시 게시용 평문 | 항상 생성 (기본값) |
| **확장본** | sshong.com 블로그용 (소제목 위계·볼드 복원, 분량 보강) | 사용자 요청 시에만 |

### 진행 방식

1. **최종 점검** — 제목 확정, 맞춤법, [TODO] 해결, 분량 확인, 평문 규칙 준수
2. **제목 확정** — 후보 3~5개 제시 → 사용자 선택 (기본본에서 제목은 첫 줄 또는 생략 가능, 사용자 확인)
3. **기본본 제공** — 복사용 코드 블록(```)으로 감싸 대화창에 제공
4. **확장본·파일 생성** — 요청 시에만. 확장본은 write-blog 연계 또는 이 스킬 내 변환(`references/publish-format.md`의 확장 규칙)

"최종 출력해줘" → 1~3 한 번에 진행.

### 제목 생성 기준

(1) 핵심 키워드 포함 (2) 15자 이내 권장 (3) 글의 톤과 일치 (4) 독자의 문제 또는 판단 기준이 드러남

### 산출물

- 기본본: 대화창 코드 블록 평문 (최종 산출물의 기본값)
- 확장본: 요청 시 대화창 텍스트 또는 write-blog 연계
- 파일(`final.docx`/`final.md`): 명시 요청 시에만 생성. 상세는 `references/output-rules.md` 참조. 문서 생성 전 반드시 docx 스킬(SKILL.md)을 읽고 따른다

---

## 스킬 간 연계

### 이 스킬의 범위

단편 콘텐츠(1회 게시 단위)를 처리한다. 기본 산출물은 페북·카톡 동시 게시용 평문. 범위 밖: 장편(책) → book-writing / 에세이 → write-essay / 강의 슬라이드 → lecture-slides / 브런치 → write-brunch / 페이스북 전용 유형 설계 → write-facebook / 블로그(sshong.com) 전용 SEO 글 → write-blog

### 타 스킬 콘텐츠를 입력으로 받을 때

| 입력 | 처리 방식 |
|------|----------|
| 책 챕터 → 칼럼형 | 핵심 논지 1~2개 추출 → 칼럼형 구조로 재구성 |
| 책 챕터 → 문서형 | 서론-본론-결론으로 재배치. 원본의 30~50% |
| 강의 스크립트 → 칼럼형 | 구어체→문어체, 시간순서→논증순서 |
| 에세이 → 칼럼형 | 경험 중심→논증 구조. 관찰→주장→근거→결론 |

### 이 스킬 → 타 스킬

이 스킬은 Step 5까지 완료. 확장본을 sshong.com에 SEO 최적화해 발행하려면 write-blog로 연계한다. 이후 변환은 대상 스킬이 담당.

---

## 문체 최소 카드 (폴백)

`hong-voice-reference` 호출이 실패했을 때만 쓴다. 정상 호출되면 이 카드를 쓰지 않는다.

```text
어조    가르치지 않되 이끈다. 설득보다 납득
시점    확인된 경험만 1인칭. 없으면 관찰·자료·개념에서 시작
전개    경험형: 실행·장면 → 기대 → 어긋남 → 현재 판단
        개념형: 문제 상황 → 개념 → 판단 기준 → 적용
리듬    같은 길이·역할 문장 3연속이면 점검. 문단 길이 균일화 금지
        단문 7어절 이하 / 중문 8~13 / 장문 14 이상 (승인 원문 실측). 규격 아님
        문단 3~5문장도 참고 범위. 문단 길이 균일화 금지
종결    채널이 정한다. 문서형·보고서는 합니다체, 페이스북·에세이는 원문 호흡에 맞춤
        한 글 안에서는 중심 종결 유지
어휘    기능·도구보다 기준·역할·구조·설계·판단
소제목  기능명 금지, 행동·판단 문장
볼드    평문 기본본 사용 안 함. 블로그 확장본만 섹션당 1~2개
마무리  현재 판단·행동·상태·남은 질문 중 하나. 요약·행동 촉구 금지
금지어  혁신적인·획기적인·완벽한·강력한·효과적으로·이를 통해·결과적으로·좋은 질문입니다
조건부  매우·쉽게·빠르게·상당히·꽤 — 실제 체감·전후 비교를 나타내면 유지
        ~할 수 있습니다 — 가능 조건이 분명하면 유지, 근거 없는 가능성만 교체
상투어  ~라고 할 수 있다·~인 것으로 보인다·중요한 역할을 한다·다양한
        ~을 통해·급변하는 시대·빼놓을 수 없다·결론적으로 ~이다
일반론  구체 사례·수치 없는 문장은 대신 채우지 않고 `구체화 필요: 위치`로 표시
교체    지운 자리는 조건·수치·비교 기준·구체 사례·실제 행동·판단으로 채운다
보존    사용자가 직접 쓴 표현, 실제 불확실성, 판단 강도, 수치·인용·고유명사
역점검  변경률 30% 초과 시 사실·문체 소유감 재대조. 50% 초과 시 근거 있는 구간만 남김
```

폴백으로 진행했으면 산출물 말미에 `문체 기준: 폴백 최소 카드 (hong-voice-reference 호출 실패)`라고 적는다. 본문에서 설명하지 않는다.

---

## 변경 이력

- 1단계 통합 (2026-08-01): `references/hongstyle.md` 삭제. 문체·표현·AI 흔적 기준을 `hong-voice-reference` 스킬 호출로 전환. Step 3·Step 4 진입부에 로드 지시 추가, 폴백 최소 카드 신설. `review-checklist.md` 점검 3과 `tone-guide.md` 감성 레이어 가드레일의 참조 경로를 `01-voice.md`로 교체.
- 1단계 보완 (2026-08-01): 실행 모드 한 줄 표기를 성공·실패 모두 필수로 전환(호출 누락 탐지용). Step 5 진입부에 `03-anti-traces.md` 최종 점검 지시 추가. Step 3 초안 자가 점검에 문체 기준 로드 확인 2항목 추가. Step 4 변경 요약에 AI 흔적 지수·실행 모드 2줄 고정.
- 2단계 통합 (2026-08-01): 중복·모순 정리. `writing-principles.md`에서 금지어 ①~⑩·AI 흔적 진단 보강 4절·단어 선택 기준 삭제(10.5KB → 6.5KB), 문장 18~25단어·문단 3~5문장을 참고 범위로 강등, 종결어미를 채널 위임으로 전환. `review-checklist.md` 점검 2를 11항목에서 8항목으로 재작성(단정 강제·전수 점검·금지어 6질문 0건 강제 삭제, 불확실성 보존·과교정 항목 신설). 문서형 마무리 `핵심 요약+CTA`를 `01-voice` §6 기준으로 교체. 감성 레이어 도입 장면에 확인된 경험 조건 부과. Step 3에서 3층을 함께 로드(과교정 원인 제거). 흔적 지수 모집단을 `03-anti-traces` §11로 단일화. 자가 점검 항목 수 표기 오류(7 → 전 항목) 수정. 실행 모드 표기를 `output-rules.md`·`review-checklist.md`에도 명시하고 점검 리포트 단독 출력 경로를 추가. 폴백 카드에 종결어미·부사 조건부 항목 보강.
- 3단계 보정 (2026-08-01): 문장 길이 기준을 `hong-voice-reference` v1.3.0 실측 구간으로 동기화. 승인 원문 100문장 실측 결과 중앙값 9어절이라 기존 `18~25단어`·`첫 문장 15단어 이내`는 홍작가 글에서 판정이 되지 않았다. 단문 7어절 이하 / 중문 8~13 / 장문 14 이상으로 교체하고, 첫 문장 후크 규칙도 7어절로 조정. `writing-principles.md`·`review-checklist.md`·`publish-format.md`·`tone-guide.md`·폴백 카드 5곳 반영.
- 이전 미해소 (해소됨): `writing-principles.md`의 금지어 ①~⑩·AI 흔적 진단 보강 4절과 `03-anti-traces.md`가 중복이다. 단정 강제(금지어 ③·점검 2-3), 문장 18~25단어·문단 3~5문장 규격, 합니다체 고정 3건은 `01-voice.md`와 충돌한다. 충돌 시 `hong-voice-reference`를 우선한다.
